<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/fileIconImageView"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_marginStart="32dp"
        app:layout_constraintBottom_toBottomOf="@+id/fileDateOfCreationTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/fileNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginTop="4dp"
        app:layout_constraintStart_toEndOf="@+id/fileIconImageView"
        app:layout_constraintTop_toTopOf="@id/fileIconImageView"
        tools:text="fileNameTextView" />

    <TextView
        android:id="@+id/fileSizeTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        app:layout_constraintStart_toEndOf="@+id/fileIconImageView"
        app:layout_constraintTop_toBottomOf="@+id/fileNameTextView"
        tools:text="fileSizeTextView" />

    <TextView
        android:id="@+id/fileDateOfCreationTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        app:layout_constraintStart_toEndOf="@+id/fileIconImageView"
        app:layout_constraintTop_toBottomOf="@+id/fileSizeTextView"
        tools:text="fileDateOfCreationTextView" />
</androidx.constraintlayout.widget.ConstraintLayout>